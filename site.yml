---

- hosts: all
  become: true
  pre_tasks:

  - name: install updates
    tags: always
    package:
      update_only: yes
      update_cache: yes

- hosts: HA_proxys
  tags: httpd,php
  become: true
  tasks:

  - name: install httpd package
    package:
      name:
        - httpd
        - php
      state: latest

- hosts: postgres_servers
  tags: shell
  become: true
  tasks: 
  - name: Display disk space
    shell: df -h 

